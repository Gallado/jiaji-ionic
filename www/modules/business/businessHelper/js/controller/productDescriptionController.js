angular.module('huatekApp.controllers').controller('ProductDescriptionCtrl', function($scope,$state) {
  var init = function(){
    $scope.ionScrollHeight = (window.innerHeight - 83) + 'px';

  };
  init();








});
